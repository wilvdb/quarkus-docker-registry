---
name: "File System Storage"
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  keywords:
    - "storage"
    - "google cloud"
    - "gcloud"
    - "gcp"
  categories:
    - "storage"
    - "data"
  status: "preview"
  config:
    - "registry.filesystem.storage."